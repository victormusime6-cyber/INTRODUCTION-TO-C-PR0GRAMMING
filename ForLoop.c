#include <stdio.h>

int main(){
    /* The for loop can repeat code a specified number of times.*/
    // for(initialization;Condition;Update)
    for(int x = 0; x < 20; x++){
    printf("%i\n",x);
}

    return 0;
}